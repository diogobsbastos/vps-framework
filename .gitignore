__pycache__/
*.pyc
.venv/
*.zip

# --- Segredos: NUNCA versionar (repo e publico) ---
gcp_teste
gcp_teste.pub
*.key
*.pem
id_ed25519*
id_rsa*
*_token*
.github_token

# Codigo embarcado (privado) — nao versionar no repo publico
default_src/
_tmp_extract/
